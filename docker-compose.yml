version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hirehand_backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
